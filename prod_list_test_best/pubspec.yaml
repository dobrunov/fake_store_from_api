name: prod_list_test
description: test single page app with BloC and freezed
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  bloc_concurrency: ^0.2.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  http: ^0.13.0
  equatable: ^2.0.3
  stream_transform: ^2.0.0
  provider: ^6.0.2

  json_serializable: ^6.1.4
  build_runner: ^2.1.7


dev_dependencies:
  freezed_annotation: ^1.1.0
  freezed: ^1.1.1

flutter:
  uses-material-design: true
